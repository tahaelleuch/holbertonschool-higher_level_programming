#!/usr/bin/python3
"""<<Exact same object>> module"""


def is_same_class(obj, a_class):
    """check obj class
    Return:
        True if it s the same False otherwise
    """
    return (type(obj) == a_class)
